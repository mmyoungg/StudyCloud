<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="dao.face.main.MtBoardDao_main">
	
	<!-- 인기멘토링 목록 조회 -->
	<select id="mtList" resultType="hashmap">
		SELECT B.*, M.*
		FROM mtboard B, member M
		WHERE B.member_no = M.member_no
		<![CDATA[AND rownum <= 8]]> 
		ORDER BY mtboard_hit DESC
	</select>
	
	<!-- 멘토링 리뷰 조회 -->
	<select id="mtReviewList" resultType="hashmap">
		SELECT R.*, M.*
		FROM mtreview R, member M
		WHERE R.member_no = M.member_no
		<![CDATA[AND rownum <= 3]]>
		ORDER BY mtreview_no DESC 
	</select>

</mapper>
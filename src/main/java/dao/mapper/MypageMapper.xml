<?xml version="1.0" encoding="UTF-8"?>
<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.face.MypageDao">

    <!-- 회원정보 수정 -->
    <update id = "userUpdate" parameterType="member">
        update MEMBER
        set member_pw = #{memberPw}, member_name = #{memberName}, member_phone = #{memberPhone}, 
       		member_email = #{memberEmail}, member_nick = #{memberNick}
        where member_no = #{memberNo}
    </update>
    
	<!-- 프로필 사진 -->
<!-- 	<select id="selectProfile" resultType="hashmap" parameterType="dto.Member"> -->
<!-- 		SELECT M.*, F.* FROM member M, fileupload F -->
<!-- 			WHERE m.member_no = f.member_no -->
<!-- 	</select>		 -->
	
	
	
    <!-- 회원 탈퇴 -->
<!-- 	<delete id="withdrawal" parameterType="member"> -->
<!-- 		delete from member -->
<!-- 		where member_pw = #{memberPw} -->
<!-- 	</delete> -->
	
    
	
	



</mapper>